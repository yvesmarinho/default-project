# requirements-airflow.txt — Airflow + providers pinned for reproducibility
# Install: pip install -r requirements-airflow.txt
# Or with uv: uv pip install -r requirements-airflow.txt
#
# NOTE: Versions pinned to latest stable at template generation time.
# Run `pip list --outdated` or `uv pip compile` periodically to update.

# ---- Core ----
apache-airflow==2.10.5
apache-airflow-providers-amazon==9.23.0
apache-airflow-providers-common-sql==1.33.0
apache-airflow-providers-http==5.6.4
apache-airflow-providers-postgres==6.6.1

# ---- Optional (uncomment as needed) ----
# apache-airflow-providers-slack==9.0.0
# apache-airflow-providers-databricks==6.9.0
# apache-airflow-providers-dbt-cloud==3.12.0
# apache-airflow-providers-google==10.27.0

# ---- Testing ----
pytest>=8.2
pytest-airflow
